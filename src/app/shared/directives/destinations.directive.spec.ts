import { DestinationsDirective } from './destinations.directive';

describe('DestinationsDirective', () => {
  it('should create an instance', () => {
    const directive = new DestinationsDirective();
    expect(directive).toBeTruthy();
  });
});
