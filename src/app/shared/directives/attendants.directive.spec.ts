import { AttendantsDirective } from './attendants.directive';

describe('AttendantsDirective', () => {
  it('should create an instance', () => {
    const directive = new AttendantsDirective();
    expect(directive).toBeTruthy();
  });
});
