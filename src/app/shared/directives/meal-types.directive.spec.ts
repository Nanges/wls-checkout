import { MealTypesDirective } from './meal-types.directive';

describe('MealTypesDirective', () => {
  it('should create an instance', () => {
    const directive = new MealTypesDirective();
    expect(directive).toBeTruthy();
  });
});
