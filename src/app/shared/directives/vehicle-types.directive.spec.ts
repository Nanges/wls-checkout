import { VehicleTypesDirective } from './vehicle-types.directive';

describe('VehicleTypesDirective', () => {
  it('should create an instance', () => {
    const directive = new VehicleTypesDirective();
    expect(directive).toBeTruthy();
  });
});
