import { HostingTypesDirective } from './hosting-types.directive';

describe('HostingTypesDirective', () => {
  it('should create an instance', () => {
    const directive = new HostingTypesDirective();
    expect(directive).toBeTruthy();
  });
});
