import { SafariExperimentsDirective } from './safari-experiments.directive';

describe('SafariExperimentsDirective', () => {
  it('should create an instance', () => {
    const directive = new SafariExperimentsDirective();
    expect(directive).toBeTruthy();
  });
});
