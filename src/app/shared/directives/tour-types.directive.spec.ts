import { TourTypesDirective } from './tour-types.directive';

describe('TourTypesDirective', () => {
  it('should create an instance', () => {
    const directive = new TourTypesDirective();
    expect(directive).toBeTruthy();
  });
});
