import { VehicleTypesFilterPipe } from './vehicle-types-filter.pipe';

describe('VehicleTypesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleTypesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
